rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Autorisation totale pour tout utilisateur connecté pour débloquer le prototype
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}