rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Ouverture totale pour le développement du prototype
    match /{document=**} {
      allow read, write: if true;
    }
  }
}